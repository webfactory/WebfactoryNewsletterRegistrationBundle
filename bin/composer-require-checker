#!/bin/bash -e

COMPOSER_REQUIRE_CHECKER_VERSION=${COMPOSER_REQUIRE_CHECKER_VERSION:-4.18.0}
if [ -t 1 ]; then TTY='--tty'; else TTY=''; fi

exec docker run --rm --init --interactive $TTY -w=/${PWD} -v $(pwd):/${PWD} ghcr.io/webfactory/composer-require-checker:$COMPOSER_REQUIRE_CHECKER_VERSION "$@"